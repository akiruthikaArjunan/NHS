Step 1:
Your final structure should look like this:
test
  java
    jobs
      config
      hooks
      pages
      steps
      utils
  resources
    features

Step 2:
  First dependency — Selenium
    Selenium is the tool that controls the browser — clicks buttons, types text, reads the page
  Second dependency — WebDriverManager
    This tool automatically downloads ChromeDriver for you. Without it, you'd have to manually download and set up ChromeDriver every time Chrome updates. WebDriverManager does it for you.
  Third dependency — Cucumber Java
    This is what lets us write tests in plain English. It reads your .feature files and understands the Given/When/Then steps.
  Fourth dependency — Cucumber JUnit Platform Engine
    This connects Cucumber to JUnit so your tests can actually run inside IntelliJ and Maven.
  Fifth dependency — JUnit Platform Suite
    This is what allows us to run the Cucumber tests as a test suite — basically the engine that powers the test runner.
  Sixth and final dependency — AssertJ
    This is what we use to check if tests pass or fail. It lets us write assertions in plain readable English
Step 3:
Look inside your <dependencies> block and tell me — do you see all 6 of these?

selenium-java
webdrivermanager
cucumber-java
cucumber-junit-platform-engine
junit-platform-suite
assertj-core

Quick recap of what we've done so far
✅ Java 21 installed
✅ IntelliJ installed
✅ Project created
✅ Folder structure created
✅ pom.xml configured with all 6 dependencies

--------------------------pom.xml is complete! --------------------------------------------------------------------------------------------------
Create the file

Right-click on the utils folder inside test/java/jobs/
Click New → Java Class
Type the name: DriverFactory
Press Enter

First — add the ThreadLocal declaration
Inside your DriverFactory class, between the { and }, type:
javaprivate static final ThreadLocal<WebDriver> driverHolder 
    = new ThreadLocal<>();


Now add the createDriver() method
This is the most important method — it creates and opens the browser.
Type this below the ThreadLocal line:


public static WebDriver createDriver(String browser, boolean headless) {
    WebDriver driver;
    
    if (browser.equalsIgnoreCase("firefox")) {
        WebDriverManager.firefoxdriver().setup();
        driver = new FirefoxDriver();
    } else {
        WebDriverManager.chromedriver().setup();
        ChromeOptions options = new ChromeOptions();
        if (headless) {
            options.addArguments("--headless=new");
        }
        options.addArguments("--window-size=1920,1080");
        driver = new ChromeDriver(options);
    }
    
    driver.manage().window().maximize();
    driverHolder.set(driver);
    return driver;
}


In simple words the whole method does this:
Someone calls createDriver("chrome", false)
     ↓
Download ChromeDriver automatically
     ↓
Open Chrome browser, maximise it
     ↓
Store it in ThreadLocal locker
     ↓
Return the browser

Now add the getDriver() method

This method simply gets the browser that was stored in the ThreadLocal locker.
Type this below the createDriver() method:
javapublic static WebDriver getDriver() {
    WebDriver driver = driverHolder.get();
    if (driver == null) {
        throw new IllegalStateException(
            "Driver not initialised. Call createDriver() first.");
    }
    return driver;
}
Before you type — understand what it does:
Someone calls getDriver()
     ↓
Look inside the ThreadLocal locker
     ↓
If locker is EMPTY → throw an error with a helpful message
If locker has a browser → return it

Now add the final method — quitDriver()
This method closes the browser and cleans up the ThreadLocal locker.
Type this below the getDriver() method:
javapublic static void quitDriver() {
    WebDriver driver = driverHolder.get();
    if (driver != null) {
        driver.quit();
        driverHolder.remove();
    }
}
Before you type — understand what it does:

Someone calls quitDriver()
     ↓
Check if there is a browser in the locker
     ↓
If YES → close the browser (driver.quit())
       → empty the locker (driverHolder.remove())
If NO  → do nothing
Let's verify DriverFactory.java is complete
Your file should have this structure:
package jobs.utils;

public class DriverFactory {

    private static final ThreadLocal<WebDriver> driverHolder...

    public static WebDriver createDriver(...) { ... }

    public static WebDriver getDriver() { ... }

    public static void quitDriver() { ... }

}

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
---------------------------------------------------------------------------------------------------------------------------------------------

Next file — BasePage.java
This is the parent file that all page files will share.
Think of it like this:

Imagine a TV remote control 📺
Every TV needs the same basic buttons — power, volume, channel
BasePage is like the standard remote — every page gets these basic buttons for free

Every page file will extend BasePage — meaning they automatically inherit all its helper methods without rewriting them.

Create the file

Right-click on the pages folder inside test/java/jobs/
Click New → Java Class
Type the name: BasePage
Press Enter

Tell me when the file is created and confirm you see package jobs.pages; at the top 👇You said: yesyes7:38 PMClaude responded: Perfect!Perfect! ✅

Understanding BasePage structure first
BasePage needs two things at the top:

driver — the browser, so every page can control it
wait — a timer that waits for elements to appear on screen

Why do we need wait?
Websites load slowly sometimes. If we try to click a button before it appears — the test crashes!
WebDriverWait says:

"Wait up to 15 seconds for this element to appear"
If it appears sooner — great, continue immediately
If it never appears in 15 seconds — fail with a clear error


First — add the constructor and variables
Inside your BasePage class type:
javaprotected final WebDriver driver;
protected final WebDriverWait wait;

protected BasePage(WebDriver driver) {
    this.driver = driver;
    this.wait = new WebDriverWait(driver, Duration.ofSeconds(15));
}
Fix any red imports with Alt + Enter — choose:

org.openqa.selenium.WebDriver
org.openqa.selenium.support.ui.WebDriverWait
java.time.Duration

Tell me when done 👇You said: donedone7:39 PMClaude responded: Perfect!Perfect! ✅

Now add the first helper — waitForVisible()
This is the most used method in the whole project. Almost every other method calls this one first.
What it does:

Takes a locator (address of an element on the page)
Waits up to 15 seconds for it to appear
Returns the element when found

Type this below the constructor:
javaprotected WebElement waitForVisible(By locator) {
    return wait.until(
        ExpectedConditions.visibilityOfElementLocated(locator));
}
Fix red imports with Alt + Enter — choose:

org.openqa.selenium.WebElement
org.openqa.selenium.By
org.openqa.selenium.support.ui.ExpectedConditions

Tell me when done 👇You said: donedone7:40 PMClaude responded: Perfect!Perfect! ✅

Now add the typeText() helper
What it does:

Finds an input field on the page
Clears anything already in it
Types the new text into it

Type this below waitForVisible():
javaprotected void typeText(By locator, String text) {
    WebElement element = waitForVisible(locator);
    element.clear();
    element.sendKeys(text);
}
Understanding it line by line:

waitForVisible(locator) — wait for the field to appear
element.clear() — delete anything already typed in it
element.sendKeys(text) — type the new text

Tell me when done 👇You said: nextnext7:41 PMClaude responded: Great!Great! ✅

Now add the click() helper
What it does:

Waits for an element to be clickable (visible AND enabled)
Then clicks it

Type this below typeText():
javaprotected void click(By locator) {
    WebElement element = wait.until(
        ExpectedConditions.elementToBeClickable(locator));
    element.click();
}
Notice the difference from waitForVisible():

visibilityOfElementLocated — just checks if element is visible
elementToBeClickable — checks if element is visible AND can be clicked

A button can be visible but disabled — elementToBeClickable handles that!
Tell me when done 👇You said: nextnext7:42 PMClaude responded: Great!Great! ✅

Now add the getText() helper
What it does:

Finds an element on the page
Returns the text inside it
Trims any extra spaces

Type this below click():
javaprotected String getText(By locator) {
    return waitForVisible(locator).getText().trim();
}
Understanding it:

waitForVisible(locator) — wait for element to appear
.getText() — read the text inside it
.trim() — remove any extra spaces at the start or end

Example use:

Find the page heading element
getText() returns "Search results for Nurse"

Tell me when done 👇You said: nextnext7:43 PMClaude responded: Great!Great! ✅

Now add the isVisible() helper
What it does:

Checks if an element is visible on the page
Returns true if visible
Returns false if not visible — without crashing!

Type this below getText():
javaprotected boolean isVisible(By locator) {
    try {
        return wait.until(
            ExpectedConditions.visibilityOfElementLocated(locator))
            .isDisplayed();
    } catch (Exception e) {
        return false;
    }
}
Why the try/catch?

If the element is NOT on the page — Selenium throws an error
Without try/catch — your test would crash
With try/catch — it simply returns false instead
Much safer! ✅

Tell me when done 👇You said: difference between wait for visble vs isvisibledifference between wait for visble vs isvisible7:44 PMClaude responded: Excellent question!Excellent question! 🎯 This is exactly the kind of thing interviewers ask!

waitForVisible() vs isVisible()

waitForVisible() — "I EXPECT this element to be there"
Looks for the element...
If found → returns it ✅
If NOT found after 15 seconds → CRASHES with an error ❌
Use it when: You are sure the element should be there.
Example:

You just clicked Search
You EXPECT results to appear
If they don't — something is genuinely wrong
You WANT it to crash and tell you!


isVisible() — "I'm just CHECKING if it's there"
Looks for the element...
If found → returns TRUE ✅
If NOT found → returns FALSE (no crash!) ✅
Use it when: The element might or might not be there.
Example:

Checking if a cookie banner appeared
It might appear, it might not
You don't want your test to crash just because there's no cookie banner!


Real life comparison
Think of it like knocking on a door 🚪
waitForVisible() is like:

Knocking and expecting someone to answer
If nobody answers → you call the police (throws error)
Used when someone MUST be home

isVisible() is like:

Knocking and checking if someone is home
If nobody answers → you just walk away (returns false)
Used when they might or might not be home


In your project
SituationWhich to useWaiting for search resultswaitForVisible()Checking for cookie bannerisVisible()Waiting for a button to type intowaitForVisible()Checking if no-results message appearedisVisible()

Does that make sense now?
Say next when ready to add the last helper — isPresent() 👇You said: yesyes7:46 PMClaude responded: Great!Great! ✅

Now add the isPresent() helper
What it does:

Checks if an element exists in the page code
Returns true or false
Does NOT wait at all

Type this below isVisible():
javaprotected boolean isPresent(By locator) {
    return !driver.findElements(locator).isEmpty();
}
Understanding it:

driver.findElements(locator) — finds ALL matching elements
Returns a list — if nothing found, list is empty
.isEmpty() — checks if list is empty
! — flips it — so returns true if list is NOT empty


isPresent() vs isVisible()
isPresent()isVisible()Waits?❌ No waiting✅ Waits 15 secondsChecks?Element exists in page codeElement is visible on screenUse when?Quick check, no waiting neededNeed to wait for element
Example:

An element can be present but hidden (display:none in CSS)
isPresent() → true (it's in the code)
isVisible() → false (you can't see it)

Tell me when done 👇You said: nextnext7:50 PMClaude responded: Great!Great! ✅
BasePage.java is complete! 🎉

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
✅ BasePage.java — shared helpers for all page files
   - waitForVisible()
   - typeText()
   - click()
   - getText()
   - isVisible()
   - isPresent()
--------------------------------------------------------------------------------------------------------------------------
Create the file

Right-click on the pages folder
Click New → Java Class
Type the name: SearchPage
Press Enter

Tell me when created and confirm you see package jobs.pages; at the top 👇You said: DoneDone8:00 PMClaude responded: Perfect!Perfect! ✅

First — extend BasePage
This is the important bit. We need to tell SearchPage to inherit all the helpers from BasePage.
Change your class declaration line from:
javapublic class SearchPage {
To:
javapublic class SearchPage extends BasePage {
What does extends mean?

SearchPage is a child of BasePage
It automatically gets all the methods we wrote in BasePage
waitForVisible(), click(), typeText() etc — all available for free!

Then add the constructor below:
javapublic SearchPage(WebDriver driver) {
    super(driver);
}
What does super(driver) mean?

Calls the parent class (BasePage) constructor
Passes the browser to BasePage so it can set everything up
Think of it like — "Hey BasePage, here's the browser, you set it up!"

Fix any red imports with Alt + Enter
Tell me when done 👇You said: nextnext8:05 PMClaude responded: Great!Great! ✅

Now add the locators
Locators are the addresses of elements on the page. They tell Selenium exactly where to find the keyword field, location field and search button.
Type these below the constructor:
javaprivate static final By KEYWORD_INPUT = By.id("keyword");
private static final By LOCATION_INPUT = By.id("location");
private static final By SEARCH_BUTTON = By.cssSelector("button[type='submit']");
Understanding each one:

By.id("keyword") — finds the element with id="keyword" in the page HTML
By.id("location") — finds the element with id="location"
By.cssSelector("button[type='submit']") — finds a button whose type is submit

Why private static final?

private — only this class uses these locators
static — belongs to the class not an object
final — these never change

Tell me when done 👇You said: DoneDone8:07 PMClaude responded: Perfect!Perfect! ✅

Now add the enterKeyword() method
This method types a keyword into the search field.
Type this below the locators:
javapublic SearchPage enterKeyword(String keyword) {
    typeText(KEYWORD_INPUT, keyword);
    return this;
}
Understanding it:

typeText(KEYWORD_INPUT, keyword) — uses the BasePage helper we wrote earlier
return this — returns the same SearchPage back

Why return this?
It allows chaining — calling multiple methods in one line:
javasearchPage.enterKeyword("Nurse").enterLocation("London")
Instead of:
javasearchPage.enterKeyword("Nurse");
searchPage.enterLocation("London");
Much cleaner to read! ✅
Tell me when done 👇You said: DoneDone8:11 PMClaude responded: Great!Great! ✅

Now add the enterLocation() method
This method types a location into the location field.
Type this below enterKeyword():
javapublic SearchPage enterLocation(String location) {
    typeText(LOCATION_INPUT, location);
    return this;
}
Notice:

Almost identical to enterKeyword()
Only difference is it uses LOCATION_INPUT locator instead
Also returns this for chaining

Tell me when done 👇You said: DoneDone8:11 PMClaude responded: Great!Great! ✅

Now add the clickSearch() method
This method clicks the search button and returns the results page.
Type this below enterLocation():
javapublic SearchResultsPage clickSearch() {
    click(SEARCH_BUTTON);
    return new SearchResultsPage(driver);
}
Understanding it:

click(SEARCH_BUTTON) — uses BasePage helper to click the button
return new SearchResultsPage(driver) — after clicking search, we land on the results page
So we return a new SearchResultsPage object ready to use

You will see SearchResultsPage in red — that's fine! We haven't created that file yet. We will create it next.
Tell me when done 👇You said: DoneDone8:13 PMClaude responded: Perfect!Perfect! ✅

Now add the last method — isKeywordInputVisible()
This method checks if the search page has loaded correctly by checking if the keyword field is visible.
Type this below clickSearch():
javapublic boolean isKeywordInputVisible() {
    return isVisible(KEYWORD_INPUT);
}
Understanding it:

Uses the isVisible() helper from BasePage
Returns true if the keyword field is on screen
We will use this in our tests to confirm the page loaded correctly

Tell me when done 👇You said: nextnext8:14 PMClaude responded: Great!Great! ✅
SearchPage.java is complete! 🎉

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
✅ BasePage.java — shared helpers for all page files
✅ SearchPage.java — keyword, location, search button
------------------------------------------------------------------------------------------------------------------------------------------------------
Next file — SearchResultsPage.java
This represents the results page after clicking search.
It needs to handle:

Checking if job results appeared
Checking if a no results message appeared
Sorting results
Pagination (next page)
Clicking the first result


Create the file

Right-click on the pages folder
Click New → Java Class
Type the name: SearchResultsPage
Press Enter

Tell me when created and confirm you see package jobs.pages; at the top 👇You said: NextNext8:16 PMClaude responded: Did you create the file?Did you create the file?
Just confirm you can see SearchResultsPage.java in your pages folder and package jobs.pages; at the top 👇You said: yesyes8:17 PMClaude responded: Perfect!Perfect! ✅

First — extend BasePage and add constructor
Just like SearchPage, this class needs to extend BasePage.
Change your class declaration to:
javapublic class SearchResultsPage extends BasePage {
Then add the constructor:
javapublic SearchResultsPage(WebDriver driver) {
    super(driver);
}
Fix any red imports with Alt + Enter
Tell me when done 👇You said: nextnext8:17 PMClaude responded: Great!Great! ✅

Now add the locators
These are the addresses of elements on the results page.
Type these below the constructor:
javaprivate static final By JOB_CARDS = By.cssSelector("[data-test='search-result']");
private static final By NO_RESULTS = By.cssSelector("[data-test='no-results']");
private static final By SORT_DROPDOWN = By.cssSelector("select[name='sortBy']");
private static final By NEXT_PAGE = By.cssSelector("[data-test='next-page']");
private static final By RESULTS_COUNT = By.cssSelector("[data-test='result-count']");
Understanding each one:

JOB_CARDS — each individual job listing card
NO_RESULTS — the message shown when nothing is found
SORT_DROPDOWN — the dropdown to sort results
NEXT_PAGE — the next page pagination link
RESULTS_COUNT — the total count text like "152 jobs found"

Tell me when done 👇You said: DoneDone8:18 PMClaude responded: Perfect!Perfect! ✅

Now add the hasResults() method
This method checks if any job cards are visible on the page.
Type this below the locators:
javapublic boolean hasResults() {
    try {
        wait.until(ExpectedConditions.or(
            ExpectedConditions.presenceOfElementLocated(JOB_CARDS),
            ExpectedConditions.presenceOfElementLocated(NO_RESULTS)
        ));
    } catch (Exception e) {
        // neither appeared - just continue
    }
    return !driver.findElements(JOB_CARDS).isEmpty();
}
Understanding it:

First waits for EITHER job cards OR no results message to appear
ExpectedConditions.or() — waits for one OR the other
Then checks if job cards list is NOT empty
Returns true if jobs found, false if not

Fix red imports with Alt + Enter
Tell me when done 👇You said: nextnext8:20 PMClaude responded: Great!Great! ✅

Now add the getResultCount() method
This method counts how many job cards are on the current page.
Type this below hasResults():
javapublic int getResultCount() {
    return driver.findElements(JOB_CARDS).size();
}
Understanding it:

findElements() — returns a list of ALL matching elements
.size() — counts how many are in the list
Returns a number like 10, 25 etc

Simple but very useful — we can use this to verify the right number of results appear.
Tell me when done 👇You said: bextbext8:21 PMClaude responded: Great!Great! ✅

Now add the hasNoResultsMessage() method
This method checks if a "no results" message is shown on the page.
Type this below getResultCount():
javapublic boolean hasNoResultsMessage() {
    return isVisible(NO_RESULTS);
}
Understanding it:

Uses isVisible() from BasePage
Returns true if the no results message is on screen
Returns false if it is not there

When would this be used?

When someone searches for something that doesn't exist
Like searching for "xyzabc123" — no jobs would match
We expect to see a no results message
This method confirms it appeared ✅

Tell me when done 👇You said: nextnext9:01 PMClaude responded: Great!Great! ✅

Now add the getResultsCountText() method
This method reads the results count text shown on the page — like "152 jobs found".
Type this below hasNoResultsMessage():
javapublic String getResultsCountText() {
    if (isVisible(RESULTS_COUNT)) {
        return getText(RESULTS_COUNT);
    }
    return "";
}
Understanding it:

First checks if the results count element is visible
If yes — reads and returns the text
If no — returns empty string instead of crashing

Why check first before getting text?

If results count is not on page and we call getText() directly — it crashes!
By checking first with isVisible() — we handle it safely ✅

Tell me when done 👇You said: Next PleaseNext Please9:04 PMClaude responded: Great!Great! ✅

Now add the sortBy() method
This method selects an option from the sort dropdown.
Type this below getResultsCountText():
javapublic SearchResultsPage sortBy(String optionText) {
    Select dropdown = new Select(waitForVisible(SORT_DROPDOWN));
    dropdown.selectByVisibleText(optionText);
    wait.until(ExpectedConditions.presenceOfElementLocated(JOB_CARDS));
    return this;
}
Understanding it:

new Select() — wraps the dropdown element so we can interact with it
waitForVisible(SORT_DROPDOWN) — waits for dropdown to appear
selectByVisibleText(optionText) — selects the option by the text you see on screen
Waits for job cards to reload after sorting
return this — returns same page for chaining

Fix red imports with Alt + Enter — choose:

org.openqa.selenium.support.ui.Select

Tell me when done 👇You said: nextnext9:05 PMClaude responded: Great!Great! ✅

Now add the hasNextPage() method
This method checks if a next page link is available.
Type this below sortBy():
javapublic boolean hasNextPage() {
    return isVisible(NEXT_PAGE);
}
Understanding it:

Uses isVisible() from BasePage
Returns true if next page link is on screen
Returns false if we are on the last page

When would this be used?

Before clicking next page — we first check it exists
If it doesn't exist and we try to click it — test would crash!
So we always check first ✅

Tell me when done 👇You said: nextnext9:06 PMClaude responded: Great!Great! ✅

Now add the goToNextPage() method
This method clicks the next page link and waits for new results to load.
Type this below hasNextPage():
javapublic SearchResultsPage goToNextPage() {
    click(NEXT_PAGE);
    wait.until(ExpectedConditions.presenceOfElementLocated(JOB_CARDS));
    return this;
}
Understanding it:

click(NEXT_PAGE) — uses BasePage helper to click next page
wait.until(...) — waits for new job cards to load
return this — returns same page for chaining

Why wait after clicking?

Clicking next page triggers a new page load
If we immediately try to read results — they might not be there yet!
Waiting ensures results are fully loaded before continuing ✅

Tell me when done 👇You said: nextnext9:07 PMClaude responded: Great!Great! ✅

Now add the clickFirstResult() method
This method clicks the first job card and returns the job detail page.
Type this below goToNextPage():
javapublic JobDetailPage clickFirstResult() {
    List<WebElement> cards = wait.until(
        ExpectedConditions.visibilityOfAllElementsLocatedBy(JOB_CARDS));
    WebElement firstCard = cards.get(0);
    WebElement link = firstCard.findElement(By.cssSelector("a"));
    link.click();
    return new JobDetailPage(driver);
}
Understanding it:

Waits for ALL job cards to be visible
cards.get(0) — gets the very first card (index 0 = first item)
Finds the link inside that card
Clicks it
Returns a new JobDetailPage — because we land there after clicking

You will see JobDetailPage in red — that's fine! We create that file next.
Fix red imports with Alt + Enter
Tell me when done 👇You said: NextNext9:09 PMClaude responded: Great!Great! ✅

Now add the last method — isPageFullyLoaded()
This method checks if the results page has loaded completely.
Type this below clickFirstResult():
javapublic boolean isPageFullyLoaded() {
    return getCurrentUrl().contains("search") 
        && isPresent(By.tagName("main"));
}
Understanding it:

getCurrentUrl() — gets the current browser URL
.contains("search") — checks URL has "search" in it
isPresent(By.tagName("main")) — checks main content area exists
Both must be true for page to be considered fully loaded

Why two checks?

URL alone isn't enough — page might still be loading
Checking for "main" tag confirms actual content is there ✅

Tell me when done 👇You said: getCurrentUrl where it exists?getCurrentUrl where it exists?9:10 PMClaude responded: Great question!Great question! 🎯

Where does getCurrentUrl() come from?
Remember when we wrote BasePage.java?
We didn't add getCurrentUrl() as a helper there — it actually comes directly from WebDriver!

But wait — how can we use it in SearchResultsPage?
Because of this chain:
SearchResultsPage 
    extends BasePage
        has WebDriver driver
            driver.getCurrentUrl()
So we could write it as:
javadriver.getCurrentUrl()

Let's fix this properly
Go back to BasePage.java and add this helper method:
javaprotected String getCurrentUrl() {
    return driver.getCurrentUrl();
}
This way:

BasePage wraps driver.getCurrentUrl()
All page files can call getCurrentUrl() cleanly
No need to write driver.getCurrentUrl() everywhere ✅


Do two things:

Add getCurrentUrl() to BasePage.java
Come back to SearchResultsPage.java

Tell me when done 👇You said: DoneDone9:12 PMClaude responded: Perfect!Perfect! ✅
Now go back to SearchResultsPage.java and add the isPageFullyLoaded() method we were just writing:
javapublic boolean isPageFullyLoaded() {
    return getCurrentUrl().contains("search") 
        && isPresent(By.tagName("main"));
}
Now it will find getCurrentUrl() from BasePage — no red underline! ✅
Tell me when done 👇You said: DoneDone9:12 PMClaude responded: Brilliant!Brilliant! SearchResultsPage.java is complete! 🎉

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
✅ BasePage.java — shared helpers for all page files
✅ SearchPage.java — keyword, location, search button
✅ SearchResultsPage.java — results, sort, pagination

Next file — JobDetailPage.java
This is the simplest page file — it represents the page you see after clicking a job listing.
It just needs to check:

Did we land on the right page?
Is the job title visible?

---------------------------------------------------------------------------------------------------------------------------

Next file — JobDetailPage.java
This is the simplest page file — it represents the page you see after clicking a job listing.
It just needs to check:

Did we land on the right page?
Is the job title visible?


Create the file

Right-click on the pages folder
Click New → Java Class
Type the name: JobDetailPage
Press Enter

Tell me when created and confirm you see package jobs.pages; at the top 👇You said: nextnext9:15 PMClaude responded: Did you create the file?Did you create the file?
Just confirm you can see JobDetailPage.java in your pages folder and package jobs.pages; at the top 👇You said: yesyes9:15 PMClaude responded: Perfect!Perfect! ✅

First — extend BasePage and add constructor
Just like the other page files, extend BasePage and add the constructor.
But this one has something extra — we wait for the URL to change to a job detail page as soon as it is created.
Change your class declaration to:
javapublic class JobDetailPage extends BasePage {
Then add the constructor:
javapublic JobDetailPage(WebDriver driver) {
    super(driver);
    wait.until(ExpectedConditions.urlContains("job"));
}
Understanding urlContains("job"):

As soon as we land on a job detail page
The URL changes to something like /job/12345
We wait for that URL change to confirm we are on the right page
If it never happens — test fails with a clear error ✅

Fix red imports with Alt + Enter
Tell me when done 👇You said: DoneDone9:16 PMClaude responded: Great!Great! ✅

Now add the locators
Type these below the constructor:
javaprivate static final By JOB_TITLE = By.cssSelector("h1");
private static final By JOB_CONTENT = By.cssSelector("main");
Understanding each one:

JOB_TITLE — the main heading of the job — always an h1 tag
JOB_CONTENT — the main content area of the page

Tell me when done 👇You said: NextNext9:17 PMClaude responded: Great!Great! ✅

Now add the isDisplayed() method
This method checks if the job detail page has loaded correctly.
Type this below the locators:
javapublic boolean isDisplayed() {
    return isVisible(JOB_TITLE) || isVisible(JOB_CONTENT);
}
Understanding it:

Checks if EITHER the job title OR the main content is visible
|| means OR — only one needs to be true
Returns true if page has loaded correctly ✅

Tell me when done 👇You said: nextnext9:17 PMClaude responded: Great!Great! ✅

Now add the getJobTitle() method
This method reads and returns the job title from the page.
Type this below isDisplayed():
javapublic String getJobTitle() {
    return getText(JOB_TITLE);
}
Understanding it:

Uses getText() from BasePage
Finds the h1 heading on the page
Returns the text inside it
Example: "Staff Nurse — London NHS Trust"

Tell me when done 👇You said: nextnext9:18 PMClaude responded: Great!Great! ✅

Now add the last method — isOnJobDetailUrl()
This method confirms the URL looks like a job detail page.
Type this below getJobTitle():
javapublic boolean isOnJobDetailUrl() {
    String url = getCurrentUrl();
    return url.contains("/job/") 
        || url.contains("/vacancy/")
        || url.contains("jobId");
}
Understanding it:

Gets the current URL
Checks if it contains any of these patterns:

/job/ — most common job detail URL pattern
/vacancy/ — another possible pattern
jobId — some sites use this in their URL


Returns true if any one of them matches ✅

Tell me when done 👇You said: nextnext9:19 PMClaude responded: Brilliant!Brilliant! JobDetailPage.java is complete! 🎉

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
✅ BasePage.java — shared helpers for all page files
✅ SearchPage.java — keyword, location, search button
✅ SearchResultsPage.java — results, sort, pagination
✅ JobDetailPage.java — job title, URL check

------------------------------------------------------------------------------------------------------------------------------------





