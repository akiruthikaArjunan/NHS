Step 1:
Your final structure should look like this:
test
  java
    jobs
      config
      hooks
      pages
      steps
      utils
  resources
    features

Step 2:
  First dependency — Selenium
    Selenium is the tool that controls the browser — clicks buttons, types text, reads the page
  Second dependency — WebDriverManager
    This tool automatically downloads ChromeDriver for you. Without it, you'd have to manually download and set up ChromeDriver every time Chrome updates. WebDriverManager does it for you.
  Third dependency — Cucumber Java
    This is what lets us write tests in plain English. It reads your .feature files and understands the Given/When/Then steps.
  Fourth dependency — Cucumber JUnit Platform Engine
    This connects Cucumber to JUnit so your tests can actually run inside IntelliJ and Maven.
  Fifth dependency — JUnit Platform Suite
    This is what allows us to run the Cucumber tests as a test suite — basically the engine that powers the test runner.
  Sixth and final dependency — AssertJ
    This is what we use to check if tests pass or fail. It lets us write assertions in plain readable English
Step 3:
Look inside your <dependencies> block and tell me — do you see all 6 of these?

selenium-java
webdrivermanager
cucumber-java
cucumber-junit-platform-engine
junit-platform-suite
assertj-core

Quick recap of what we've done so far
✅ Java 21 installed
✅ IntelliJ installed
✅ Project created
✅ Folder structure created
✅ pom.xml configured with all 6 dependencies

--------------------------pom.xml is complete! --------------------------------------------------------------------------------------------------
Create the file

Right-click on the utils folder inside test/java/jobs/
Click New → Java Class
Type the name: DriverFactory
Press Enter

First — add the ThreadLocal declaration
Inside your DriverFactory class, between the { and }, type:
javaprivate static final ThreadLocal<WebDriver> driverHolder 
    = new ThreadLocal<>();


Now add the createDriver() method
This is the most important method — it creates and opens the browser.
Type this below the ThreadLocal line:


public static WebDriver createDriver(String browser, boolean headless) {
    WebDriver driver;
    
    if (browser.equalsIgnoreCase("firefox")) {
        WebDriverManager.firefoxdriver().setup();
        driver = new FirefoxDriver();
    } else {
        WebDriverManager.chromedriver().setup();
        ChromeOptions options = new ChromeOptions();
        if (headless) {
            options.addArguments("--headless=new");
        }
        options.addArguments("--window-size=1920,1080");
        driver = new ChromeDriver(options);
    }
    
    driver.manage().window().maximize();
    driverHolder.set(driver);
    return driver;
}


In simple words the whole method does this:
Someone calls createDriver("chrome", false)
     ↓
Download ChromeDriver automatically
     ↓
Open Chrome browser, maximise it
     ↓
Store it in ThreadLocal locker
     ↓
Return the browser

Now add the getDriver() method

This method simply gets the browser that was stored in the ThreadLocal locker.
Type this below the createDriver() method:
javapublic static WebDriver getDriver() {
    WebDriver driver = driverHolder.get();
    if (driver == null) {
        throw new IllegalStateException(
            "Driver not initialised. Call createDriver() first.");
    }
    return driver;
}
Before you type — understand what it does:
Someone calls getDriver()
     ↓
Look inside the ThreadLocal locker
     ↓
If locker is EMPTY → throw an error with a helpful message
If locker has a browser → return it

Now add the final method — quitDriver()
This method closes the browser and cleans up the ThreadLocal locker.
Type this below the getDriver() method:
javapublic static void quitDriver() {
    WebDriver driver = driverHolder.get();
    if (driver != null) {
        driver.quit();
        driverHolder.remove();
    }
}
Before you type — understand what it does:

Someone calls quitDriver()
     ↓
Check if there is a browser in the locker
     ↓
If YES → close the browser (driver.quit())
       → empty the locker (driverHolder.remove())
If NO  → do nothing
Let's verify DriverFactory.java is complete
Your file should have this structure:
package jobs.utils;

public class DriverFactory {

    private static final ThreadLocal<WebDriver> driverHolder...

    public static WebDriver createDriver(...) { ... }

    public static WebDriver getDriver() { ... }

    public static void quitDriver() { ... }

}

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
---------------------------------------------------------------------------------------------------------------------------------------------

Create the file

Right-click on the pages folder inside test/java/jobs/
Click New → Java Class
Type the name: BasePage
Press Enter


First — add the constructor and variables
Inside your BasePage class type:
javaprotected final WebDriver driver;
protected final WebDriverWait wait;

protected BasePage(WebDriver driver) {
    this.driver = driver;
    this.wait = new WebDriverWait(driver, Duration.ofSeconds(15));
}
Fix any red imports with Alt + Enter — choose:

org.openqa.selenium.WebDriver
org.openqa.selenium.support.ui.WebDriverWait
java.time.Duration

  
Now add the isVisible() helper
What it does:

Checks if an element is visible on the page
Returns true if visible
Returns false if not visible — without crashing!

Type this below getText():
javaprotected boolean isVisible(By locator) {
    try {
        return wait.until(
            ExpectedConditions.visibilityOfElementLocated(locator))
            .isDisplayed();
    } catch (Exception e) {
        return false;
    }
}

Now add the isPresent() helper
What it does:

Checks if an element exists in the page code
Returns true or false
Does NOT wait at all

Type this below isVisible():
javaprotected boolean isPresent(By locator) {
    return !driver.findElements(locator).isEmpty();
}
Understanding it:

driver.findElements(locator) — finds ALL matching elements
Returns a list — if nothing found, list is empty
.isEmpty() — checks if list is empty
! — flips it — so returns true if list is NOT empty

Great! ✅
BasePage.java is complete! 🎉

Quick recap so far
✅ pom.xml — all 6 dependencies
✅ ConfigReader.java — browser, headless, URL settings
✅ DriverFactory.java — creates, gets, closes the browser
✅ BasePage.java — shared helpers for all page files
   - waitForVisible()
   - typeText()
   - click()
   - getText()
   - isVisible()
   - isPresent()
--------------------------------------------------------------------------------------------------------------------------





